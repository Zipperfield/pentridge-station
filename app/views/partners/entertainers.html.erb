<% content_for :meta_tags do %>
  <title>Entertainers</title>
  <meta name="description" content="Musicians, DJs, and other entertainers that you can book for your private event.">
<% end %>
<%= render 'shared/navbar', page: "entertainers"%>
<%= render 'feed', backgroundColor: 'bg-red-300'  %>
