<input class="carousel-open hidden" type="radio" id="carousel-<%=currSlide%>" name="carousel" aria-hidden="true" hidden="" <%="checked=checked" if currSlide==1%>>
<div class="carousel-item absolute hidden h-72">
	<a <%=get_article_href(link) %>>
		<div class="block h-full w-full bg-teal-darkest bg-cover bg-center"
			 style= "background-image: url(<%= get_photo_url(photo)%>">
		  	<div class="w-full h-full bg-teal-darkest bg-opacity-70 transition duration-1000 hover:bg-opacity-0 flex justify-center">
				<div class="opacity-100 text-white flex flex-col justify-center">
		  	 		<div class="press-header">
		    			<%=get_text_content(publisher, "Article #{currSlide} Publisher")%>
					</div>
					<div class="press-line"></div>
					<div class="italic text-lg self-center text-center w-32 sm:w-2/3 ">
						<%=get_text_content(headline, "A headline or quote from article #{currSlide} to build social proof.")%>
					</div>
				</div>
			</div>
		</div>
	</a>
</div>
<label for="carousel-<%=prevSlide%>"class= "<%=class_names("to-the-left-arrow","prev", "control-#{currSlide}")%>" >‹</label>
<label for="carousel-<%=nextSlide%>"class="<%=class_names("to-the-right-arrow","next","control-#{currSlide}")%>">›</label>
		