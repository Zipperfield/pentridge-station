document.addEventListener('turbolinks:load', () => {
    if (document.getElementById('navbar').getAttribute('page') != 'home') {
        return
    }

    // Before I start this I Should research prebuilt solutions

    // if small screen
    //  toggle first article 

    // loop through each article
    //      toggle hidden and flex 
    //      wait 
    //      toggle again

    // if large screen return to normal?
});
